# Keep the CPU wheel index first so pip does not select PyTorch's CUDA build.
--index-url https://download.pytorch.org/whl/cpu
--extra-index-url https://pypi.org/simple

fastapi==0.115.12
uvicorn[standard]==0.34.0
laya==0.3.3
pytest==8.3.5
httpx==0.28.1
